# Generated fixture policy

`job-valid.json` contains metadata only. Its input is the tiny PNG constructed
in memory by `forensic_image_community.fixture_data.generated_rgb_png`; the
media bytes are never stored in Git. All other image fixtures are produced in
memory by `tests/helpers.py`. No scraped, private or user media is used.
