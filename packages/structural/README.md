# Structural analysis package

CPU-only structural media inspection for Phase 3. The package verifies an
immutable evidence object, invokes optional metadata tools through one bounded
shell-free runner, normalizes results, and renders deterministic JSON/HTML.

It contains no detector inference, network client, model download, cloud code,
or evidence mutation path.
